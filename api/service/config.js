const mysql_config = {
    db: {
        connectoinLimit: 100,
        host: "localhost",
        user: "root",
        password: "mauFJcuf5dhRMQrjj",
        database: "NATURE",
        insecureAuth : true
    },
    listPerPage: 100,
}

module.exports = {
    mysql_config
}

// check mysql node git repo for more detail set up.